{
  "name": "conduit-monorepo",
  "version": "0.1.1",
  "private": true,
  "packageManager": "pnpm@9.15.9",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "clean": "pnpm -r clean",
    "lint": "eslint .",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "typecheck": "tsc -b --pretty false",
    "test": "vitest run --workspace vitest.unit.config.ts && vitest run --workspace vitest.integration.config.ts && vitest run --workspace vitest.security.config.ts && node --test scripts/*.test.js",
    "test:unit": "vitest run --workspace vitest.unit.config.ts",
    "test:integration": "vitest run --workspace vitest.integration.config.ts",
    "test:e2e": "playwright test",
    "test:security": "vitest run --workspace vitest.security.config.ts",
    "test:coverage": "vitest run --config vitest.coverage.config.ts --workspace vitest.coverage.workspace.ts --coverage",
    "docs:dev": "node scripts/docs-workspace.js dev",
    "docs:build": "node scripts/docs-workspace.js build",
    "distribution:build": "pnpm --filter @conduit/distribution build",
    "distribution:pack": "pnpm distribution:build && pnpm --filter @conduit/distribution run pack",
    "distribution:test": "pnpm distribution:build && pnpm --filter @conduit/distribution test:install",
    "release:check": "node scripts/release-check.js",
    "daemon:start": "pnpm --filter @conduit/daemon start",
    "daemon:dev": "pnpm --filter @conduit/daemon dev",
    "mcp:start": "pnpm --filter @conduit/mcp-server start",
    "conduit:doctor": "node packages/cli/bin/conduit.js --json doctor"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/node": "^26.2.0",
    "@vitest/coverage-v8": "4.1.10",
    "esbuild": "0.28.2",
    "eslint": "^10.8.1",
    "prettier": "^3.9.6",
    "ts-node": "^10.9.2",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
