{
  "name": "@conduit/extension",
  "version": "0.1.0",
  "main": "dist/background.js",
  "scripts": {
    "build": "node build.js",
    "typecheck": "tsc --noEmit",
    "package": "node build.js && node package.js"
  },
  "dependencies": {
    "@conduit/browser-core": "workspace:*",
    "@conduit/protocol": "workspace:*"
  },
  "devDependencies": {
    "@types/chrome": "^0.2.2",
    "esbuild": "0.28.1"
  }
}
