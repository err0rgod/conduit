name: Browser E2E

on:
  push:
    branches: [main]
    paths:
      - 'apps/daemon/**'
      - 'packages/cli/**'
      - 'packages/config/**'
      - 'packages/daemon-client/**'
      - 'packages/protocol/**'
      - 'packages/security/**'
      - 'tests/e2e/**'
      - 'playwright.config.ts'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/browser-e2e.yml'
  pull_request:
    branches: [main]
    paths:
      - 'apps/daemon/**'
      - 'packages/cli/**'
      - 'packages/config/**'
      - 'packages/daemon-client/**'
      - 'packages/protocol/**'
      - 'packages/security/**'
      - 'tests/e2e/**'
      - 'playwright.config.ts'
      - 'package.json'
      - 'pnpm-lock.yaml'
      - '.github/workflows/browser-e2e.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: browser-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  chromium:
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - name: Checkout
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
      - name: Checkout pinned extension
        uses: actions/checkout@d23441a48e516b6c34aea4fa41551a30e30af803 # v6
        with:
          repository: err0rgod/conduit-extension
          ref: f4211eb7ceb1ba15d955fda5ee51e15887d83b84
          path: conduit-extension
      - name: Install pnpm
        uses: pnpm/action-setup@b0f76dfb45f55f8421693e4803ac7bb65143bd34 # v6
      - name: Set up Node
        uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
        with:
          node-version: 24
          cache: pnpm
      - run: pnpm install --frozen-lockfile
      - name: Install extension dependencies
        working-directory: conduit-extension
        run: pnpm install --frozen-lockfile
      - name: Build workspace dependencies
        run: pnpm build
      - name: Build pinned extension
        working-directory: conduit-extension
        run: pnpm build
      - name: Install Chromium
        run: pnpm exec playwright install chromium
      - name: Run extension E2E
        env:
          CONDUIT_EXTENSION_PATH: ${{ github.workspace }}/conduit-extension/apps/extension/dist
        run: xvfb-run --auto-servernum pnpm test:e2e
      - name: Upload failure report
        if: failure()
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7
        with:
          name: playwright-report
          path: |
            playwright-report/
            test-results/
          if-no-files-found: ignore
          retention-days: 7
